// Centralized app configuration values.
// Note: Secrets are loaded from api_keys.dart (gitignored).

import 'package:ecoalert/config/api_keys.dart';

final String mapboxAccessToken = ApiKeys.mapbox;
