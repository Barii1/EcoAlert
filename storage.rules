rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Test mode — open access. Lock down before production.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
